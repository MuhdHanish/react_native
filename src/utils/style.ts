import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    textStyle: {
        color: "white",
        backgroundColor: "teal",
        fontSize: 24,
        fontWeight: "bold",
        margin: 20,
        padding: 20
    }
});