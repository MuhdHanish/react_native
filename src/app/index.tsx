import React from "react";
import { View } from "react-native";
import { UpdateArray } from "../components";

export default function Index() {
    return (
        <View style={{ padding: 10 }}>
            <UpdateArray />
        </View>
    );
};
