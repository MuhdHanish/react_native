import React from "react";
import { View } from "react-native";
import { ConditionalStatement } from "../components";

export default function Index() {
    return (
        <View style={{ padding: 10 }}>
            <ConditionalStatement />
        </View>
    );
};
